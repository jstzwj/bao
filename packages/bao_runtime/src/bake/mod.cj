// 翻译自 Bun: src/runtime/bake/mod.rs
// Bake 模块入口。Rust 源文件负责重新导出 FrameworkRouter、生产构建、DevServer 及共享类型。

package bao_runtime.bake

// Cangjie packages cannot publicly import themselves or child packages from a same-named aggregator.
